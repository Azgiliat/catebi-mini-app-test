# Local mock API env is loaded from the repository root by the dev:mock-api script.
# Use ../../.env.example as the source template and copy it to ../../.env.local.

# Production / deployed function secret name:
# TELEGRAM_BOT_TOKEN_SECRET
# ADMIN_UIDS_SECRET
# Set it with:
# yarn firebase functions:secrets:set TELEGRAM_BOT_TOKEN_SECRET
# yarn firebase functions:secrets:set ADMIN_UIDS_SECRET
